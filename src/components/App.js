import React from 'react';
import { Switch, BrowserRouter, Route } from 'react-router-dom';

const App = () => {
  return (
    <BrowserRouter>
      <div>
        <Switch>
        </Switch>
      </div>
    </BrowserRouter>
  );
};

export default App;
